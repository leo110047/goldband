# Review contract regression fixtures

These snapshots preserve the former Goldband root and loop contract cases used
by schema, provider-selection, Python, and CI-evidence regression tests. They are
not loaded by project review discovery. Active project contracts are registered
in the durable Goldband state root under `review-contracts`.
